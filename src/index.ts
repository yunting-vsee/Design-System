/**
 * @vsee/ui — VSee Design System library entry.
 *
 * v0.1.0 is CSS-only. Consumers import the stylesheets directly:
 *
 *   @import '@vsee/ui/tokens.css';
 *   @import '@vsee/ui/styles.css';
 *
 * These string exports exist as a discoverability aid (e.g. tools that
 * want to resolve the asset path via `import.meta.resolve` can do so).
 * Components land in v0.2.0+ as named exports from this module.
 */
export const tokensCss = "./tokens.css";
export const stylesCss = "./styles.css";
